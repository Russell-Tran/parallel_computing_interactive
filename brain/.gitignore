# Local reference material only — slide screenshots, PDFs, scratch notes.
# Nothing in here belongs in the repo, but the directory itself is tracked
# so the folder exists on a fresh clone.
*
!.gitignore
